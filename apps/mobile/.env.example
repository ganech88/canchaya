EXPO_PUBLIC_NHOST_SUBDOMAIN=your-project-subdomain
EXPO_PUBLIC_NHOST_REGION=sa-east-1

# Mapbox: dejar vacío en Expo Go (cae a placeholder SVG). Para dev build con
# EAS, setear con un access token público (pk.xxx) y agregar el download token
# en app.json plugin config.
EXPO_PUBLIC_MAPBOX_TOKEN=

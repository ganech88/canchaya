import Svg, { Path, Circle } from 'react-native-svg'
import { createNativeIcon } from '@canchaya/ui/native'

export const Icon = createNativeIcon({ Svg, Path, Circle })
export type { IconName } from '@canchaya/ui/icons'
